# from models.eqm_user_session import EqmUserSession
# from Cryptodome.Random import get_random_bytes


async def start_encryption():
    raise NotImplementedError
    # key, nonce = get_random_bytes(32), get_random_bytes(8)
    # session.writer.write(key)
    # session.writer.write(nonce)
    # await session.writer.drain()
    # session.encryption_key = (key, nonce)
